apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-service.fullname" . }}-test-connection"
  labels:
    {{- include "helm-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-service.fullname" . }}:8080']
  restartPolicy: Never
