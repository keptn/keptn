{
  "$schema": "http://json-schema.org/draft-07/schema",
  "properties": {
    "remoteControlPlane": {
      "type": "object",
      "required": [
        "enabled"
      ],
      "properties": {
        "enabled": {
          "type": "boolean"
        }
      },
      "if": {
        "properties": {
          "enabled": {
            "const": true
          }
        }
      },
      "then": {
        "properties": {
          "api": {
            "type": "object",
            "required": [
              "hostname",
              "stageFilter",
              "token"
            ],
            "properties": {
              "hostname": {
                "pattern": "^[a-z][a-z0-9-.]{2,63}$"
              },
              "protocol": {
                "enum": [
                  "http",
                  "https"
                ]
              },
              "apiValidateTls": {
                "type": "boolean"
              },
              "stageFilter": {
                "pattern": "^[a-z][a-z0-9-.]{2,63}$"
              },
              "token": {
                "pattern": "^[a-z][a-z0-9-.]{2,63}$"
              }
            }
          }
        }
      }
    },
    "helmservice": {
      "type": "object",
      "required": [
        "image"
      ],
      "properties": {
        "image": {
          "properties": {
            "repository": {
              "pattern": "^[a-z][a-z0-9-./]{2,63}$"
            },
            "pullPolicy": {
              "enum": [
                "IfNotPresent",
                "Always"
              ]
            }
          }
        },
        "service": {
          "properties": {
            "enabled": {
              "type": "boolean"
            }
          }
        }
      }
    },
    "distributor": {
      "type": "object",
      "required": [
        "image"
      ],
      "properties": {
        "image": {
          "properties": {
            "repository": {
              "pattern": "^[a-z][a-z0-9-./]{2,63}$"
            },
            "pullPolicy": {
              "enum": [
                "IfNotPresent",
                "Always"
              ]
            }
          }
        }
      }
    }
  }
}