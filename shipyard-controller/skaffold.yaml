apiVersion: skaffold/v2beta28
kind: Config
build:
  local:
    useBuildkit: true
  artifacts:
    - image: docker.io/mowies/shipyard-controller
      docker:
        dockerfile: Dockerfile
        target: production
        buildArgs:
          debugBuild: true
deploy:
  kubectl:
    defaultNamespace: keptn-test-2736-1
    manifests:
      - deploy/service.yaml
