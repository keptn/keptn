service:
  http:
    type: ClusterIP
    port: 3000
    clusterIP:
  ssh:
    type: ClusterIP
    port: 22
    clusterIP:

cache:
  builtIn:
    enabled: true

gitea:
  config:
    server:
      OFFLINE_MODE: true
    database:
      DB_TYPE: mysql
      HOST: gitea-mysql:5432
      NAME: gitea
      USER: gitea
      PASSWD: gitea

postgresql:
  enabled: false

mysql:
  enabled: true
