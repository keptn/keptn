service:
  http:
    type: ClusterIP
    port: 3000
    clusterIP:
  ssh:
    type: ClusterIP
    port: 22
    clusterIP:

#persistence:
#  enabled: false

gitea:
  database:
    builtIn:
      postgresql:
        enabled: false
      mysql:
        enabled: true