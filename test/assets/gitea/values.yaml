service:
  http:
    type: ClusterIP
    port: 3000
    clusterIP:
  ssh:
    type: ClusterIP
    port: 22
    clusterIP:

cache:
  builtIn:
    enabled: true

gitea:
  config:
    server:
      OFFLINE_MODE: true
    database:
      DB_TYPE: sqlite3

memcached:
  enabled: false

postgresql:
  enabled: false
