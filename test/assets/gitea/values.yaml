service:
  http:
    type: ClusterIP
    port: 3000
    clusterIP:
  ssh:
    type: ClusterIP
    port: 22
    clusterIP:

gitea:
  database:
    builtIn:
      postgresql:
        enabled: false
      mariadb:
        enabled: true
